#include<bits/stdc++.h>
using namespace std;
#define inf 10000000
#define lli long long
#define lld long double
#define vli vector<long long>
#define vld vector<long double>

void solve(){
	lli n,x;
	cin>>n>>x;
	vli a(n),b(n);
	for(lli i=0;i<n;i++){
		cin>>a[i][0];
		a[i][1]=i;
		mp[i]
	}

	for(lli i=0;i<n;i++){
		cin>>b[i][0];
		b[i][1]=i;
	}
	sort(a.begin(),a.end());
	sort(b.begin(),b.end());
	for(lli i=)

}
int main(){
	lli t;
	cin>>t;
	while(t--)solve();
	return 0;
}